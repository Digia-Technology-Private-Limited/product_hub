name: producthub_demo
description: Comprehensive Digia UI SDK integration example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Digia UI SDK (uncomment when using real SDK)
  digia_ui: ^1.4.0  

  dio: ^5.0.3
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  assets:
    - assets/
    - assets/images/
  

